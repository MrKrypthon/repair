import { IconPackages } from '@tabler/icons-react';

const inventory = { id: 'inventory', title: 'Inventario', type: 'group', children: [{ id: 'inventory-list', title: 'Piezas y consumibles', type: 'item', url: '/inventory', icon: IconPackages }] };

export default inventory;
