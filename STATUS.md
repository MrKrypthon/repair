# Estado del proyecto

Última actualización: 2026-09-07

## Objetivo

Construir una aplicación responsive para centralizar la operación de un taller: clientes, equipos, órdenes de servicio, diagnóstico, agenda, inventario, cobros, documentación técnica y métricas.

## Estado actual

### Completado

- Se inspeccionaron las especificaciones funcionales, el roadmap y el modelo de datos inicial.
- Se creó `electronica-tech-app` a partir de la variante Vite del template Berry.
- Se conservaron los componentes visuales, layout, tema, navegación y dashboard del template como base.
- Se adaptaron nombre, idioma, metadatos y configuración base para Electrónica Tech.
- Se dejó configurado PostgreSQL local mediante `infra/docker-compose.yml`.
- Se documentó la arquitectura inicial y el contrato de módulos backend.
- Se corrigió y fijó la compatibilidad de `framer-motion` con `motion-dom` para que el build sea reproducible.
- Se reemplazó el dashboard demostrativo por un dashboard inicial del taller con métricas, órdenes recientes y acciones rápidas.
- Se adaptó la navegación principal para usar `/dashboard` como entrada del sistema.
- Se añadieron las vistas iniciales de clientes y órdenes de servicio.
- Se añadió el formulario de recepción para crear una nueva orden con cliente, equipo, falla y prioridad.
- Se creó el backend NestJS con Prisma y endpoints iniciales para clientes y órdenes.
- Se creó y aplicó la migración inicial de PostgreSQL.
- Clientes, órdenes y nueva orden ya consumen la API mediante `src/api/client.js`.
- Se validó en PostgreSQL la creación transaccional de un cliente, equipo, orden y primer historial de estado.
- Se añadió `dev.sh` y `npm run dev:all` para levantar BBDD, migraciones, API y frontend desde un solo comando.
- El script detecta si el puerto de la API está ocupado y ajusta `PORT` y `VITE_API_URL` automáticamente.
- Se añadió el detalle de orden con historial y actualización transaccional de estados.
- Se añadió diagnóstico técnico con causa probable y checklist de pruebas del equipo.
- Se añadió asignación de técnico responsable por orden.
- Se añadió portal público de seguimiento por token para clientes.
- Se añadió cierre formal de entrega con fecha y transición auditable a `ENTREGADO`.
- El portal público permite autorizar o rechazar el presupuesto mediante el token de seguimiento.
- La decisión pública actualiza automáticamente el estado de la orden y registra la transición en el historial.
- Se añadió centro de notificaciones internas para autorizaciones de presupuesto y entregas.
- Se añadieron notas técnicas y mediciones reutilizables dentro de cada orden.
- Se añadieron búsqueda y filtros por estado y prioridad en el listado de órdenes.
- Se añadió exportación CSV de las órdenes filtradas.
- El módulo de clientes ahora permite registrar equipos desde la ficha del cliente.
- Se añadió edición de datos del cliente desde su ficha.
- La ficha de cliente preselecciona al cliente al iniciar una nueva orden.
- Se añadieron acciones de llamada, correo y WhatsApp desde la ficha de cliente.
- Se añadió exportación CSV de clientes filtrados.
- La ficha de cliente permite crear una cita con el cliente preseleccionado.
- Se añadió archivado lógico de clientes para conservar historial sin mantenerlos en el listado operativo.
- Se añadió gestión de usuarios y roles del MVP con creación protegida para administradores.
- Se añadió fecha estimada de entrega en la recepción y detalle de órdenes.
- Nueva orden puede reutilizar equipos existentes del cliente sin duplicarlos.
- Se añadió proveedores como siguiente módulo operativo del roadmap, vinculado conceptualmente al inventario.
- Inventario ahora incluye proveedor asociado en sus consultas y visualización.
- El alta de piezas permite seleccionar proveedor directamente.
- Se añadió alta y ajuste de stock desde la interfaz de inventario.
- MVP operativo completado; inventario y proveedores quedan listos para la siguiente etapa de órdenes de compra.
- Se inició la siguiente etapa con órdenes de compra a proveedores y recepción futura de piezas.
- Las órdenes de compra ahora pueden recibirse y aumentar stock mediante una transacción con movimiento de inventario.
- Las órdenes de compra ahora tienen transición explícita `DRAFT -> ORDERED -> RECEIVED`.
- Las órdenes de compra permiten múltiples líneas de piezas y cantidades.
- Las órdenes de compra permiten varias piezas y cantidades por proveedor.
- El detalle de la orden permite copiar y abrir el enlace público de seguimiento del cliente.
- Inventario ahora permite crear piezas y ajustar entradas/salidas de stock desde la interfaz.
- El flujo de recepción permite reutilizar equipos existentes del cliente.
- El formulario de nueva orden ahora permite definir fecha estimada de entrega.
- Se añadió cálculo de ganancia estimada y margen al dashboard.
- La migración `add_public_tracking` fue aplicada preservando las órdenes existentes.
- Se añadió autenticación JWT, roles iniciales y usuario administrador de desarrollo.
- La API de clientes y órdenes ahora exige `Authorization: Bearer <token>`.
- El login del frontend ya autentica contra `POST /api/auth/login` y conserva el token en la sesión del navegador.
- Las rutas principales del frontend requieren sesión y la API valida permisos por rol en operaciones sensibles.
- Se añadieron `RequireAuth`, `Roles` y `RolesGuard` para proteger navegación y operaciones por perfil.
- Se añadió el módulo de pagos con anticipos, pagos parciales/finales y métodos de cobro.
- El detalle de orden permite registrar y consultar cobros desde la interfaz.
- Se añadió desglose de presupuesto, autorización y cálculo automático del total estimado.
- La migración `add_budget` fue aplicada en PostgreSQL y frontend/backend compilan correctamente.
- Se añadió inventario con existencias, mínimos, movimientos de entrada/salida y pantalla de consulta.
- La migración `add_inventory` fue aplicada en PostgreSQL.
- Se añadió consumo de piezas por orden con descuento transaccional de inventario y registro de movimiento.
- El detalle de la orden permite seleccionar piezas disponibles y registrar cantidades utilizadas.
- La migración `add_order_parts` fue aplicada en PostgreSQL.
- Se añadió agenda con eventos de recepción, entrega, citas y trabajos programados.
- La migración `add_appointments` fue aplicada en PostgreSQL.
- Se añadió base técnica con documentos, soluciones, mediciones y búsqueda por modelo/palabra clave.
- La migración `add_technical_knowledge` fue aplicada en PostgreSQL.
- El dashboard consume métricas reales mediante `GET /api/analytics/dashboard`, con fallback visual para desarrollo.
- El dashboard calcula ganancia estimada y margen a partir de órdenes y piezas consumidas.
- Se recuperaron gráficas ApexCharts en el dashboard: actividad semanal y distribución de órdenes por estado.
- La tabla de órdenes recientes del dashboard consume ahora `/api/service-orders`.
- Se añadió el formulario de alta de clientes conectado a `POST /api/customers`.
- Se añadió la ficha de cliente con equipos registrados e historial de órdenes.
- Se añadió `GET /api/auth/me` y logout real desde el menú de usuario.
- Se añadieron DTOs con `class-validator` para validar los endpoints principales antes de persistir datos.
- Se configuró Jest con pruebas unitarias iniciales para login válido y rechazo de credenciales.
- Se añadió una prueba e2e HTTP para login y protección de recursos contra PostgreSQL local.
- La prueba e2e cubre también la persistencia integrada de cliente, orden, pieza consumida y pago.
- Se añadió CI para validar base de datos, pruebas unitarias, pruebas e2e y builds en cada cambio.
- Se corrigió el backend: faltaba `backend/tsconfig.build.json`, por lo que TypeScript compilaba `src/` y `test/` juntos y emitía a `dist/src/*.js` en vez de `dist/*.js`, dejando la API sin arrancar.
- Se añadió almacenamiento de fotografías y documentos por orden de servicio usando almacenamiento compatible con S3 (MinIO en desarrollo, vía `infra/docker-compose.yml`).
- El detalle de la orden permite subir (JPG/PNG/WEBP/PDF, hasta 10MB), previsualizar y eliminar archivos adjuntos.

### En progreso

- Definir wireframes y navegación final de clientes, equipos y órdenes.
- Definir estados, roles, permisos y reglas financieras definitivas.

### Pendiente

- Ampliar la cobertura de pruebas unitarias e integración más allá de auth y el flujo principal.

## Decisiones tecnológicas

| Área | Decisión | Motivo |
| --- | --- | --- |
| Frontend | React 19 + Vite + JavaScript | Encaja con el template recibido y permite iterar rápido |
| UI | Material UI + Berry Vite | Reutiliza la base visual existente y es responsive |
| Backend | Node.js + NestJS | Modularidad, validación y crecimiento hacia API móvil |
| Persistencia | PostgreSQL | Relaciones fuertes, auditoría y consultas de negocio |
| Archivos | S3/R2; MinIO local | Mantener fotos y documentación fuera de la BBDD |
| Arquitectura | Monolito modular | Menor complejidad inicial sin cerrar la evolución futura |

## Alcance del MVP

1. Autenticación, usuarios y roles.
2. Clientes y equipos.
3. Órdenes de servicio y folios únicos.
4. Estados e historial auditable.
5. Diagnóstico y presupuesto.
6. Pagos y dashboard básico.

Inventario, agenda, notificaciones y base de conocimiento técnica quedan en fases posteriores, salvo que sean necesarios para cerrar el flujo principal.

## Entidades iniciales

`User`, `Customer`, `Device`, `ServiceOrder`, `StatusHistory`, `Payment`, `InventoryItem`, `InventoryMovement`, `OrderPart`, `TechnicalDocument` y `TechnicalNote`.

## Roles y permisos

| Acción | Admin | Recepción | Técnico |
| --- | --- | --- | --- |
| Crear/editar clientes, equipos, órdenes | ✅ | ✅ | — |
| Editar datos de una orden (falla, prioridad, equipo, entrega estimada) | ✅ | ✅ | ✅ |
| Cambiar estado de una orden | ✅ | ✅ | ✅ |
| Diagnóstico, notas técnicas, fotos/documentos | ✅ | fotos/documentos | ✅ |
| Cargar desglose de presupuesto (piezas/mano de obra) | ✅ | ✅ | ✅ |
| **Autorizar o rechazar un presupuesto** | ✅ | ✅ | ❌ |
| Consumir piezas dentro de una orden (descuenta stock) | ✅ | ✅ | ✅ |
| **Ajuste manual de stock** (entrada/salida fuera de una orden) | ✅ | ✅ | ❌ |
| Registrar pagos | ✅ | ✅ | — |
| **Ver dashboard financiero** (ganancia, margen, cobros) | ✅ | ✅ | ❌ |
| Gestionar usuarios y roles | ✅ | — | — |
| Archivar clientes | ✅ | — | — |

El técnico, al iniciar sesión, va directo a "Órdenes de servicio" en lugar del dashboard, y no ve la opción de Dashboard en el menú.

## Decisiones pendientes

- ¿La primera instalación será para un único taller o debe soportar multiempresa desde el inicio?
- ¿Qué moneda, zona horaria e impuestos se utilizarán?
- ¿Se almacenará algún dato sensible del dispositivo o solo se marcará como entregado al técnico?
- ¿Qué proveedor de correo, WhatsApp y almacenamiento se usará en producción?

## Registro de sesiones

### 2026-09-07

- Análisis inicial de documentación y template.
- Selección de React/Vite para frontend y propuesta Node/NestJS + PostgreSQL para backend.
- Creación del proyecto base y configuración inicial.
- Verificación exitosa de `npm run build` y `docker compose ... config`.
- Primera iteración visual del dashboard operativo, todavía con datos locales de demostración.
- Primera iteración del flujo `Clientes -> Nueva orden -> Órdenes de servicio`.
- Backend compilado y migración `init` aplicada sobre PostgreSQL local en el puerto `5433`.
- Se reservó el puerto `5433` para PostgreSQL porque `5432` ya estaba ocupado por otro proyecto del entorno.

### 2026-09-10

- Diagnóstico y corrección del backend: faltaba `backend/tsconfig.build.json`, lo que rompía `node dist/main` al mezclar `src/` y `test/` en el build. Se agregó el archivo y se validó login extremo a extremo.
- Se añadió el módulo de almacenamiento (`StorageService`, S3-compatible) y MinIO local (`infra/docker-compose.yml`, servicios `minio` y `minio-init` con bucket de descarga pública).
- Se añadió el modelo `OrderAttachment` y la migración `add_order_attachments`.
- Se añadieron endpoints `POST/DELETE /api/service-orders/:folio/attachments` con validación de tipo (JPG/PNG/WEBP/PDF) y tamaño (10MB máx).
- El detalle de orden ahora permite subir, previsualizar y eliminar fotos y documentos.
- Se corrigió un bug existente en `detail.jsx`: se leía `order.payments` antes de comprobar que `order` no fuera `null`, lo que podía romper el render inicial.
- Se probó el flujo completo (subida, URL pública, listado, borrado, rechazo de tipos no permitidos) contra la base de datos real.

### 2026-09-11

- Se creó el repositorio en GitHub (`MrKrypthon/repair`) con el commit inicial y se enlazó como `origin`.
- Se corrigió el CI tras el primer push: `bitnami/minio:latest` ya no existe en Docker Hub; se reemplazó por un contenedor MinIO levantado manualmente (`docker run`) más creación de bucket con `minio/mc`, verificado localmente antes de subir el fix.
- Se añadió edición de orden desde la interfaz (`PATCH /api/service-orders/:folio`): falla reportada, prioridad, fecha estimada de entrega y datos del equipo (categoría, marca, modelo, color, número de serie, IMEI).
- Se detectó y corrigió un gap real en las pruebas e2e: el `ValidationPipe` global de `main.ts` nunca se aplicaba a la app de pruebas, por lo que la validación de DTOs no se ejercitaba de verdad.
- Se definieron y aplicaron los roles y permisos: solo Admin/Recepción pueden autorizar o rechazar presupuestos, ver el dashboard financiero y hacer ajustes manuales de inventario; Técnico conserva la carga de costos, el cambio de estado y el consumo de piezas dentro de una orden. Ver tabla completa en la sección "Roles y permisos".
- Se ocultó el Dashboard del menú y la redirección de login para el rol Técnico, que ahora entra directo a Órdenes de servicio.
- Se resolvieron las 16 vulnerabilidades altas marcadas por `npm audit` (7 backend, 9 frontend). Backend: se fijaron `multer` y `deepmerge-ts` a versiones no vulnerables vía `overrides` (evitando el downgrade a NestJS 7 y Prisma 6.12 que sugería `--force`). Frontend: se actualizaron `lodash-es`, `react-router`, `react-router-dom` y `vite`, y se eliminó `prettier-eslint-cli` (dependencia de desarrollo sin uso real que arrastraba un `minimatch` vulnerable). `npm audit` queda en 0 vulnerabilidades en ambos proyectos.
- Se corrigió el manejo de sesión expirada: el cliente API ahora detecta un 401 (JWT vencido a las 8h), limpia la sesión guardada y redirige a `/pages/login` en vez de mostrar el mensaje genérico de "no se pudo conectar con la API" en cada vista.
- Se rediseñó el inventario con estilo de tienda: cada pieza tiene foto (subida a MinIO/S3, `POST/DELETE /api/inventory/:id/image`), la vista pasó de tabla a una grilla de tarjetas con imagen, y se añadió búsqueda real contra el backend (`GET /api/inventory?q=...`) con filtro por categoría en el cliente. Solo Admin/Recepción pueden subir, reemplazar o borrar la foto de un producto.
- Se rediseñó la página pública de seguimiento (`/tracking/:token`): ahora muestra una línea de tiempo visual (stepper) con la etapa actual de la reparación, las fotos del equipo subidas durante el proceso (con vista ampliada al hacer clic), y se actualiza sola cada 25s sin recargar. Los estados terminales (Cancelado, Sin reparación) muestran un aviso en vez del stepper. El endpoint público solo expone fotos, nunca documentos internos.
- Queda pendiente, y explícitamente fuera de esta iteración, enviar notificaciones reales (correo/WhatsApp/SMS) al cliente cuando cambia el estado; requiere elegir un proveedor (decisión ya listada en STATUS.md).
- Se llevó el mismo stepper visual al detalle interno de la orden (arriba de todo, antes del contenido), para que el equipo del taller vea de un vistazo en qué etapa está el equipo, igual que lo ve el cliente. La lógica del stepper se compartió entre ambas vistas vía `src/utils/serviceOrderJourney.js`.
- Rebranding: el proyecto pasa a llamarse **FixTrack** (antes "Electrónica Tech", nombre heredado del template Berry). Se creó un logo nuevo (ícono con check + acento, en `src/ui-component/Logo.jsx`, y `favicon.svg`), y se quitaron todos los rastros del template original: pie de página con créditos a CodedThemes/enlaces a Berry, la tarjeta de "Upgrade your plan" (marketing de CodedThemes) en el menú de usuario, el enlace de "Documentation" a los docs de Berry, y el componente de landing page de Berry sin usar. Se renombraron `title`/metadatos de `index.html`, el `README.md`, el `name` de ambos `package.json` (`fixtrack-app` / `fixtrack-api`) y las claves de `localStorage` (`fixtrack-token` / `fixtrack-user`, `fixtrack-config`). **Nota:** al cambiar las claves de `localStorage`, cualquier sesión ya iniciada en el navegador queda como "no autenticada" — hay que volver a iniciar sesión una vez. Quedaron intactos, a propósito, identificadores técnicos internos no visibles para el usuario (el bucket de MinIO `electronica-tech-attachments` y el dominio del usuario admin de desarrollo `admin@electronicatech.local`).
- Se añadió el módulo de Finanzas (`GET /api/finance/summary?from=&to=`, solo Admin): ingresos reales (suma de pagos registrados), gastos reales (suma de órdenes de compra ya recibidas de proveedores), ganancia neta, indicador de números rojos/verdes, margen, tendencia mensual de los últimos 12 meses y el listado de movimientos (pagos y compras) que la componen. Nueva vista `/finance` con tarjetas de resumen, gráfica de barras ingresos-vs-gastos y tabla de movimientos; el menú "Finanzas" solo aparece para Admin, y la ruta redirige a quien no lo sea.
- Se creó `backend/prisma/demo-seed.cjs` con datos demo cercanos a la realidad: 3 usuarios (2 técnicos, 1 recepción), 3 proveedores, 14 piezas de inventario con categorías reales, 3 órdenes de compra recibidas, 12 clientes con equipo, y 18 órdenes de servicio repartidas en todos los estados posibles (con diagnóstico, presupuesto, piezas consumidas, pagos, historial y un par de citas/documentación técnica). Está calibrado para dar finanzas netas en positivo.
- **Hallazgo relevante**: las pruebas e2e escriben datos reales (con sufijos `E2E ...`) directamente en la base indicada por `DATABASE_URL` sin limpiarlos después. Correrlas contra la base de desarrollo local (en vez de una base de pruebas separada) deja esos registros mezclados permanentemente con los datos reales/demo — así fue como aparecieron ~38 clientes/órdenes y ~10 usuarios de prueba en la base local durante esta sesión, que hubo que limpiar antes de que las finanzas demo dieran en positivo. Documentado el riesgo en `backend/README.md`; sigue pendiente decidir si vale la pena aislar `test:e2e` a una base de datos propia para no depender de correrlo con cuidado.
- Se añadió descarga de comprobante/cotización en PDF desde el detalle de la orden (botón "Descargar PDF" junto al estado): folio, cliente, equipo, falla reportada, diagnóstico, desglose de presupuesto, estado de autorización y enlace de seguimiento público, generado 100% en el cliente con `jspdf`/`jspdf-autotable` (sin tocar el backend). Verificado generando PDFs reales a partir de datos de la app (con y sin diagnóstico) y revisando el resultado visualmente. **Nota de tamaño**: `jspdf` incluye `html2canvas` y `dompurify` aunque no se usen (son dependencias estáticas de la librería), así que el chunk de "detalle de orden" creció de ~22KB a ~440KB (~144KB gzip); como la ruta es lazy-loaded no afecta la carga inicial de la app, pero es la fuente de la mayor parte del peso de esa pantalla.
- Se reemplazó la lista de "Próximos eventos" de la Agenda por un calendario real (`@fullcalendar/react` + day-grid/time-grid/list/interaction, todas fijadas en `6.1.21` porque las variantes del plugin aún no publican para el core v7). Vistas mes/semana/lista con selector en la barra de herramientas, en español. Seleccionar un rango en el calendario prellena el formulario de "Crear evento"; hacer clic en un evento abre un diálogo con sus datos y, si está programado, permite marcarlo como completado o cancelado (`PATCH /api/appointments/:id/status`, nuevo). Los eventos se colorean por tipo (recepción/entrega/cita/trabajo).
- Se añadieron alertas automáticas de stock bajo: cuando una pieza cruza su stock mínimo (por ajuste manual en Inventario o por consumirse dentro de una orden), se crea una notificación interna tipo `WARNING` visible para todo el equipo. Solo se avisa en el momento en que el stock *cruza* el umbral (de por encima a por debajo), no en cada movimiento subsecuente mientras ya está bajo, para no saturar el centro de notificaciones. De paso, el centro de notificaciones ahora colorea cada aviso (chip y borde) según su tipo (`INFO`/`SUCCESS`/`WARNING`/`ERROR`), que antes se ignoraba visualmente. Verificado en vivo el cruce de umbral, la ausencia de duplicados, y el disparo tanto desde ajuste manual como desde consumo de piezas en una orden.
- Se añadió garantía de reparaciones. Al marcar una orden como entregada (ahora vía un diálogo, no un solo clic) se puede indicar días de garantía, que calculan `warrantyExpiresAt`. El detalle de la orden y el seguimiento público muestran si la garantía está vigente o vencida, hasta qué fecha, y el comprobante en PDF también la incluye. Si sigue vigente, aparece el botón "Abrir reclamo de garantía", que crea una nueva orden (folio `GA-...`) para el mismo cliente y equipo, vinculada a la original vía `warrantyForOrderId` (relación propia de `ServiceOrder`, migración `add_warranty`); tanto la orden original como el reclamo muestran el enlace entre ambas. El backend rechaza reclamos si la orden no está entregada o si la garantía ya venció. Probado en vivo el flujo completo y los tres casos de rechazo (no entregada, sin garantía, garantía vencida); se añadió cobertura e2e para el mismo flujo.
- Se añadió búsqueda global (`GET /api/search?q=...`, mín. 2 caracteres, hasta 5 resultados por categoría en clientes/órdenes/inventario). Se reemplazó el `SearchSection` del header — hasta ahora un placeholder decorativo del template Berry que nunca hizo nada — por una barra de búsqueda real, siempre visible arriba (con versión colapsable en móvil), con desglose de resultados por categoría y navegación directa al hacer clic (cliente → ficha, orden → detalle, pieza → inventario filtrado por SKU vía `/inventory?q=...`, para lo cual el inventario ahora lee el query inicial de la URL). Verificado en vivo por nombre de cliente, folio de orden y nombre/SKU de pieza; se añadió cobertura e2e.
- Se añadió el reporte de productividad por técnico (`GET /api/analytics/technicians?from=&to=`, solo Admin): por cada técnico, órdenes cerradas, órdenes activas, tiempo promedio de reparación (días entre recepción y entrega) e ingresos/ganancia generados (mismo criterio de "ganancia" que el dashboard: cobrado menos costo de piezas consumidas). Nueva vista `/productivity` con gráfica de barras y tabla, con el mismo filtro Este mes/Este año/Todo que Finanzas. El menú "Finanzas" se renombró a "Reportes" y ahora agrupa Finanzas y Productividad, ambos visibles solo para Admin. Verificado en vivo con datos demo reales y con filtro de fecha; se añadió cobertura e2e (incluyendo que Técnico/Recepción reciben 403).
- Se añadió historial de precios de inventario (migración `add_inventory_price_history`, modelo `InventoryPriceHistory`). Se agregó `PATCH /api/inventory/:id` (antes no existía forma de editar una pieza ya creada) que registra el cambio cuando costo o precio se modifican; y se corrigió que recibir una orden de compra sobreescribía el costo de la pieza en silencio, sin dejar rastro — ahora también queda en el historial, con el folio de la compra como referencia. Nueva acción "Editar" y "Historial de precios" en cada tarjeta de Inventario (botón con ícono, junto a "Ajustar stock"), con un diálogo que lista fecha, costo anterior→nuevo, precio anterior→nuevo y origen (edición manual o recepción de compra). Verificado en vivo ambos disparadores y que editar sin tocar costo/precio no genera un registro; se añadió cobertura e2e para los tres casos.
- Se añadió código QR al comprobante en PDF (`qrcode`, 100% en el cliente, sin tocar el backend): apunta al mismo enlace de seguimiento público que ya se incluía como texto, para que el cliente lo escanee con el celular en vez de escribirlo. Se ubica en la esquina inferior derecha del comprobante, alineado con el bloque de seguimiento. La generación del PDF pasó a ser asíncrona (`generateOrderPdf` ahora es `async`); el botón "Descargar PDF" del detalle de la orden muestra "Generando..." mientras tanto. Verificado generando un PDF real (con Node, ya que no hay navegador disponible en este entorno) y revisando el resultado visualmente: el QR se ve nítido y bien alineado.
- Se añadió el módulo de cotizaciones (modelos `Quotation`/`QuotationItem`, migración `add_quotations`). Una cotización tiene cliente, equipo (existente o nuevo), falla reportada, conceptos con cantidad/precio unitario, vigencia opcional y notas, con folio `COT-...` y flujo de estado Borrador → Enviada → Aprobada/Rechazada (terminal: una vez que el cliente responde ya no se puede editar ni recotizar, hay que crear una nueva). Solo Admin/Recepción pueden crear y cambiar estado; cualquier rol autenticado puede consultarlas. Una cotización **aprobada** se puede convertir en una orden de servicio nueva (`POST /api/quotations/:folio/convert`), que reutiliza cliente y equipo, usa la falla reportada como falla de la orden y el total cotizado como costo estimado (en "Otros cargos"); la cotización queda marcada como `CONVERTED` y enlazada a la orden generada en ambos sentidos. Nueva sección "Cotizaciones" en el menú (Operación) con listado filtrable por estado, formulario de creación con conceptos añadibles uno a uno, y detalle con las acciones de estado, conversión a orden y botón "Descargar PDF" (`generateQuotationPdf`, mismo estilo del comprobante de orden pero sin QR, pensado para enviarse al cliente por correo/WhatsApp antes de que autorice el trabajo). Verificado en vivo el flujo completo con curl (crear, editar en borrador, enviar, aprobar, convertir, y los rechazos esperados: no se puede editar/convertir fuera de los estados correctos, no se puede convertir dos veces, Técnico recibe 403 al crear) y el PDF generado visualmente con Node; se añadió cobertura e2e para el mismo flujo.
- Se añadió modo oscuro. El template Berry ya traía definidos (sin usar) todos los tokens de color para un tema oscuro (`darkPaper`, `darkLevel1/2`, `darkTextPrimary/Secondary/Title`, `darkPrimary*`, `darkSecondary*` en `themes/theme/default.js`) y la infraestructura de cambio de tema de MUI (`colorSchemes`, `useColorScheme`, persistencia en `localStorage` bajo la clave `theme-mode`), pero solo el esquema `light` estaba registrado. Se construyó el esquema `dark` en `themes/palette.jsx` reutilizando esos tokens (fondo, texto, primario/secundario específicos de oscuro; error/advertencia/éxito se mantienen iguales en ambos modos, como ya insinuaba la falta de variantes oscuras para esos tokens) y se remapeó la escala `grey` para que seguir usándola como "panel sutil" (patrón ya usado en varias vistas, incluida Cotizaciones) diera un resultado coherente en modo oscuro en vez de cajas casi blancas sobre fondo oscuro. Se ajustaron las sombras (`custom-shadows.jsx`) para usar una base negra fija en oscuro, ya que antes tomaban `grey[900]` — que en el nuevo mapeo es un tono claro pensado para texto, no para sombras. Se agregó un botón de sol/luna en el header (`ThemeModeSection`, junto a notificaciones) que alterna entre claro y oscuro con un clic, basado en `useColorScheme()` de MUI; el modo por defecto sigue siendo "system" (respeta el tema del sistema operativo la primera vez) y la preferencia del usuario persiste entre sesiones. Se revisó el resto de la app en busca de colores fijos que pudieran romperse en oscuro (fondos hardcodeados en `#fff`, tarjetas, layout, login) — los únicos hallados son insignias de color sólido con texto blanco fijo (tipo de cita, categorías), que son correctos en ambos modos por diseño. No se pudo verificar visualmente en un navegador real (no hay Chrome disponible en este entorno, `npx playwright install chrome` requiere permisos de root); se verificó con build + lint limpios y revisión exhaustiva de que cada token de color oscuro referenciado existe y es un hex válido.
- Se pulió el PDF de cotizaciones (`generateQuotationPdf.js`). Antes el contenido se escribía a coordenadas fijas sin comprobar el límite de la página: una falla reportada larga, notas largas o muchos conceptos podían salirse del borde inferior en silencio. Se agregó un helper `ensureSpace` que agrega una página nueva cuando no cabe el siguiente bloque, y `Página X de Y` en la esquina inferior de cada hoja. También se agregó: una insignia de estado (Borrador/Enviada/Aprobada/Rechazada/Convertida) junto al folio, con la fecha de vigencia marcada en rojo y "(VENCIDA)" si ya pasó y la cotización sigue abierta (mismo criterio que el chip "Vencida" del detalle); una sección "Condiciones" con tres notas estándar (moneda, disponibilidad de piezas, tiempo de reparación sujeto a autorización); y una línea de firma de aceptación del cliente con fecha, pensada para cuando se imprime y se hace firmar en persona. Probado generando PDFs con Node (sin navegador disponible): un caso corto de una sola página (borrador, sin vigencia) y un caso largo de 12 conceptos con notas extensas que fuerza 2 páginas, confirmando que el salto de página cae en un punto limpio (antes de la firma, no a la mitad de una sección) y que la numeración de páginas es correcta en ambas.
- Se añadió el módulo de Configuración (`/settings`, accesible desde el menú de perfil en el header) y se limpiaron varios restos genéricos del template Berry. Backend: `User.avatarKey` (migración `add_user_avatar`) y nuevos endpoints de autoservicio en `auth` — `PATCH /api/auth/me` (nombre), `PATCH /api/auth/me/password` (requiere contraseña actual), `POST`/`DELETE /api/auth/me/avatar` (foto de perfil, mismo patrón de subida a S3 que ya usaba Inventario). Frontend: página de Configuración con foto de perfil (subir/quitar), edición de nombre, cambio de contraseña y una sección de apariencia (tipografía y radio de bordes, con botón para restablecer valores predeterminados). Se reescribió por completo el menú del avatar en el header (`ProfileSection`), que hasta ahora era contenido de ejemplo del template sin traducir ni funcional: un saludo fijo en inglés ("Good Morning"), interruptores de "Start DND Mode"/"Allow Notifications" que no hacían nada, un buscador decorativo, un ítem "Social Profile" con una insignia falsa, y un botón etiquetado "Account Settings" que en realidad cerraba la sesión (el verdadero botón de "Logout", debajo, quedaba duplicado). Ahora muestra un saludo real según la hora del día, la foto de perfil, y dos acciones que sí hacen lo que dicen: "Configuración" y "Cerrar sesión". También se eliminó el botón flotante de engranaje ("Live Customize") que abría un cajón con las mismas opciones de tipografía/radio de bordes fuera de cualquier flujo real de la app (`layout/Customization/`); esas dos opciones se movieron a la nueva página de Configuración y el componente se borró del layout, lo que además redujo el bundle principal (~150KB) al dejar de cargarse siempre en cada pantalla. Verificado en vivo con curl (actualizar nombre, cambiar contraseña con contraseña actual incorrecta/correcta y confirmar que el login refleja el cambio, subir/quitar avatar con una imagen real) y se añadió cobertura e2e para el mismo flujo.
- Se auditó el Dashboard (vista de inicio de Admin/Recepción) verificando que cada elemento tuviera una acción real detrás, y se encontraron varios que no la tenían. La fecha bajo el título estaba escrita a mano ("Martes, 7 de septiembre de 2026") y quedaba desactualizada con cada día que pasaba — ahora se calcula con `Date` y sale en español. El botón "Consultar cobros pendientes" apuntaba a `/payments`, una ruta que nunca existió (llevaba a una pantalla en blanco); se reemplazó por un botón "Revisar órdenes atrasadas" que ahora sí filtra de verdad, con un contador en vivo de cuántas hay. La gráfica de "Actividad del taller" (últimos 7 días) solo usaba el dato de hoy real y rellenaba los otros 6 días con números fijos e inventados; la de "Órdenes por estado" tenía una de sus cuatro rebanadas (Diagnóstico) siempre en 8 sin importar la realidad; y la tarjeta "Recepción del día" decía "4 equipos · 2 celulares · 1 laptop · 1 consola" sin importar lo que hubiera pasado ese día. Las tres se corrigieron extendiendo `GET /api/analytics/dashboard` con datos reales: `weeklyVolume` (recibidas/entregadas por día, últimos 7 días), `statusBreakdown` (conteo real por estado) y `todayReception` (equipos recibidos hoy por categoría). Para que "Revisar órdenes atrasadas" pudiera filtrar de verdad, el listado de Órdenes de servicio ahora soporta `?overdue=1` en la URL y un botón de alternar "Atrasadas" en sus propios filtros, con la misma regla que usa el backend (fecha estimada de entrega vencida y la orden sigue sin entregarse ni cancelarse) y una insignia "Atrasada" en cada fila que aplica. De paso se encontró y borró un clúster completo de componentes de ejemplo del template Berry (`EarningCard`, `BajajAreaChartCard`, `TotalOrderLineChartCard`, `TotalGrowthBarChart`, `PopularCard`, `TotalIncomeDarkCard`/`LightCard` y sus datos/esqueletos de carga) que nunca se importaban desde ninguna pantalla real de la app — confirmado con `grep` antes de borrar y con `npm run lint`/`build` después, sin errores. Verificado en vivo con curl comparando el conteo de atrasadas del dashboard contra un cálculo manual sobre el listado de órdenes (coincidieron en 0), y de nuevo tras crear una orden de prueba con fecha de entrega vencida (el contador subió a 1 como se esperaba); se añadió una aserción e2e sobre la forma de los nuevos campos.
- Se le dieron acción real a las tarjetas de métricas del Dashboard ("En reparación", "Listos para entregar", "Cobros registrados", "Ganancia estimada", "Órdenes atrasadas"), que hasta ahora eran solo decorativas — se veían igual que un botón pero no hacían nada al hacer clic. Ahora cada una navega al listado de Órdenes de servicio ya filtrado para mostrar exactamente lo que cuenta la tarjeta: "En reparación" filtra por los tres estados que el backend agrupa bajo ese número (`EN_REPARACION`, `EN_PRUEBAS`, `EN_DIAGNOSTICO`), "Listos para entregar" por `LISTO_ENTREGA`, y "Órdenes atrasadas" reutiliza el filtro `?overdue=1` que ya se había construido. Para esto, el listado de órdenes ahora también acepta `?status=A,B,C` en la URL como filtro adicional (independiente del selector de estado manual) y muestra un aviso "Mostrando solo: ..." con botón para quitarlo, para que quede claro por qué la lista llegó ya filtrada. "Cobros registrados" y "Ganancia estimada" llevan a Finanzas, pero solo para Admin — Recepción también ve el Dashboard pero no tiene acceso a Finanzas, así que esas dos tarjetas se quedan sin enlace (no rotas, simplemente no clicables) en vez de llevar a una pantalla de "sin permiso". Verificado en vivo con curl comparando el conteo de cada tarjeta (`inRepair`, `ready`) contra un cálculo manual sobre el listado completo de órdenes con la misma regla de agrupación — coincidieron exactamente.
- Se añadió un efecto "flotante" al pasar el cursor sobre botones y tarjetas clicables, a pedido explícito del usuario. De paso se encontró que `themes/overrides/Button.jsx` — el archivo que se supone define el estilo de `MuiButton` en todo el theme — en realidad nunca lo hacía: su contenido era una copia exacta de `Slider.jsx` (mismo `MuiSlider`, ni una línea de `MuiButton`), así que todos los botones de la app corrían con el estilo por defecto de MUI. Se reescribió con overrides reales de `MuiButton`: al pasar el cursor, se eleva 2px y gana sombra (más marcada en `contained`, sutil en `outlined`), con transición suave y sin efecto sobre botones deshabilitados. Se agregó el mismo tratamiento a `MuiIconButton` (usados en botones de editar, subir archivos, etc. por toda la app), con una elevación más discreta acorde a su tamaño. Además, `MainCard` ahora detecta automáticamente cuando se usa como enlace (`component={Link}`, patrón ya usado en las tarjetas de métricas del Dashboard) y aplica el mismo efecto de elevación + cursor de mano, sin necesidad de repetir el estilo a mano en cada pantalla que lo use — se simplificó `MetricCard` para apoyarse en esto. No fue posible verificarlo visualmente en un navegador (no hay Chrome disponible en este entorno); se verificó que el build compila limpio y que las reglas CSS nuevas (`translateY(-2px)`, `translateY(-1px) scale(1.06)`) efectivamente quedan incluidas en el bundle final.
- El usuario probó la app y reportó que los botones con borde (`outlined`, como los de "Acciones rápidas" en el Resumen del taller) no se sentían flotando. Se confirmó que el servidor de desarrollo sí estaba sirviendo el código nuevo (el cambio llegaba bien), así que el problema real era de percepción: la sombra usada para `outlined` (`customShadows.z1`, la más sutil de la escala) era casi imperceptible sobre un botón que ya tiene un borde delgado — a diferencia de los `contained`, que sí se notaban por tener relleno de color. Se igualó la sombra de `outlined` a la misma intensidad que `contained` (`z8`) y se engrosó ligeramente el borde al pasar el cursor (de 1px a 1.5px) para reforzar la sensación de "tarjeta con borde que flota". También se agregó una animación de "presión" (`scale(0.97)` con transición rápida de 80ms) al hacer clic/mantener presionado, en botones, íconos clicables y tarjetas interactivas (como las del Dashboard), para que se sienta una respuesta táctil al seleccionar el componente, no solo al pasar el cursor. Se confirmó en el bundle final que las nuevas reglas (`scale(0.97)`, `scale(0.92)`, `scale(0.98)`, `borderWidth`) quedaron incluidas.
- El usuario aclaró que se refería específicamente a las tarjetas de datos del Resumen del taller (las mismas que ya tienen acción de clic desde la sesión anterior) y pidió que "floten" igual que los botones de Acciones rápidas. Para verificar esto de verdad (no solo revisar CSS), se instaló temporalmente `puppeteer-core` en el directorio de scratchpad (fuera del proyecto, sin tocar `package.json`) y se manejó el Chromium del sistema para probar la app en vivo, con sesión iniciada, como lo vería el usuario. Esto permitió encontrar y corregir un problema real: `MuiCard` trae `overflow: hidden` por defecto en MUI, lo que recorta tanto `outline` como `box-shadow` cuando se usan para simular un halo en tarjetas (a diferencia de los botones, que no tienen ese recorte y por eso sí se veían bien). Se reemplazó el intento de halo por un cambio de `background-color` al pasar el cursor (`theme.vars.palette.action.hover`), el mismo mecanismo que ya usa el resaltado del menú lateral en esta app (`ListItemButton`), combinado con el `translateY(-2px)` de elevación. Se confirmó mediante `getComputedStyle` y `.matches(':hover')` que el navegador aplica correctamente los estilos nuevos al pasar el cursor. Sin embargo, la verificación por captura de pantalla resultó contradictoria específicamente para las tarjetas (mientras que para los botones sí se logró confirmar visualmente sin ambigüedad, con capturas mostrando el halo con claridad): las capturas de la tarjeta no mostraban ningún cambio visible pese a que los estilos computados eran correctos, incluso en su estado base sin hover. Todo apunta a una limitación del navegador headless en este entorno (aparentemente sin aceleración por GPU) para componer correctamente la superficie de elevación de `Paper`/`Card` de MUI, no a un error en el código — pero no se pudo confirmar visualmente al 100% en este entorno. Se le pidió al usuario que lo revise directamente en su navegador y reporte si el efecto sigue sin notarse ahí.
- El usuario confirmó que seguía sin verse, ni siquiera en modo claro — descartando que fuera un problema de contraste específico de modo oscuro. Se movió el hover de las tarjetas del `sx` inline al mismo mecanismo global (`theme.components`) que sí se había confirmado funcionando para los botones, usando el atributo `[href]` que generan los enlaces de React Router como selector (`themes/overrides/Card.jsx`, nuevo archivo). Se probaron tres variantes distintas (halo sutil, halo fuerte con `overflow: visible` + sombra `z16` + elevación de 4px) verificando cada una con `getComputedStyle`, `.matches(':hover')` y hasta un volcado completo de la pila de elementos en el punto central de la tarjeta (`document.elementsFromPoint`) para descartar un elemento invisible superpuesto — no se encontró ninguno. En los tres casos, los estilos computados eran correctos e inequívocos (sombra real y sustancial, cambio de fondo, transform), pero las capturas de pantalla automatizadas seguían sin mostrar ningún cambio en la tarjeta, mientras que la misma verificación sobre los botones sí mostraba el cambio con claridad. No se logró aislar la causa exacta dentro del tiempo disponible; quedó documentado como un caso abierto de posible incompatibilidad entre el navegador headless sin GPU de este entorno y la forma en que MUI compone la superficie de `Paper`/`Card`. La versión que quedó en el código es la más robusta de las probadas (mecanismo idéntico al de los botones, ya confirmado en producción visual) y es técnicamente correcta según todas las verificaciones posibles sin ojos humanos — pero su efecto real en un navegador de verdad no quedó confirmado por el asistente en esta sesión.
- Se encontró y corrigió un conflicto de especificidad CSS real (no una corazonada): el propio `sx` inline de `MainCard.jsx` sigue definiendo su regla original `':hover': { boxShadow: ... }` (heredada de antes de esta sesión), que genera `.css-XXXX-MuiCard-root:hover { box-shadow: inherit; }` para cualquier tarjeta sin la prop `boxShadow`. Como el nuevo override global usa el selector `[href]:hover` (más específico), en teoría debía ganar para `box-shadow` — y, confirmado con un volcado exacto de las reglas CSS generadas antes y después del cambio, así era: antes del fix `box-shadow` seguía resolviendo a `inherit` en el navegador pese a la mayor especificidad de mi regla (indicando que el conflicto sí estaba interfiriendo), y después del fix (se quitó la regla `:hover` redundante en `MainCard.jsx`, solo se aplica si se pasa explícitamente la prop `boxShadow`) el `box-shadow` computado pasó a ser el valor completo esperado. Con todo, la verificación por captura de pantalla siguió sin mostrar cambio visible para las tarjetas en este entorno, así que este arreglo es una corrección real y verificable pero no se confirmó que resuelva el problema que reporta el usuario.
- Se reemplazó `box-shadow`/`outline`/fondo translúcido por un cambio de `border` (de transparente a `primary.main`, 3px) combinado con una elevación más pronunciada (`translateY(-6px)`) — la técnica más a prueba de fallos posible en CSS: el borde es parte de la caja del propio elemento, nunca se recorta por `overflow` ni depende de que nada se "asome" fuera de la tarjeta. Confirmado por `getComputedStyle` que las tres propiedades (`borderColor`, `transform`, `backgroundColor`) cambian correctamente al pasar el cursor. El usuario confirmó que ya había probado con refresco forzado (Ctrl+Shift+R) y seguía sin verse, y prefirió que se siguiera ajustando sin recurrir a las herramientas de desarrollador del navegador. Queda como el mejor esfuerzo posible sin poder ver la app renderizada directamente; si tras este cambio el usuario sigue sin notar nada en absluto (ni el borde, ni la elevación, ni el cambio de fondo), el siguiente paso recomendado sería revisar si hay algo específico del entorno del usuario (extensión del navegador, configuración de accesibilidad como "reducir movimiento", zoom de página) que pudiera estar interfiriendo, ya que desde el lado del código no se encontró ninguna otra causa técnica tras múltiples verificaciones independientes.
- El usuario confirmó que el cambio de borde sí se ve — el efecto de flotar en las tarjetas del Dashboard ya funciona. Pidió el mismo efecto en las tarjetas de resumen de Finanzas ("Ingresos", "Gastos", "Ganancia neta", "Estado del periodo"), que hasta ahora eran visualmente idénticas a las del Dashboard pero no eran clicables. Siguiendo el mismo patrón, ahora cada una navega a algo real: "Ingresos" y "Ganancia neta" bajan con un ancla (`#movimientos`) hasta la tabla de movimientos de la misma página (que ya lista el detalle línea por línea); "Gastos (piezas / proveedores)" lleva a `/purchase-orders`, que es donde realmente viven los registros de esas compras; y "Estado del periodo" también baja a movimientos, para mantener las cuatro tarjetas de la fila con el mismo comportamiento interactivo. Verificado en vivo con un navegador real que las cuatro se convierten en enlaces (`<a href>`) correctos y que hacer clic en "Gastos" navega efectivamente a `/purchase-orders`.
- El usuario reportó que se veía mal, "como doble botón". Con una captura de pantalla real se encontró el problema exacto: el `Chip` de la tarjeta "Estado del periodo" ("Números verdes · Ganancia" / "Números rojos · Pérdida") es más ancho que la tarjeta en la columna más angosta de la fila (`lg:3`, la cuarta de cuatro), y como `MuiCard` recorta su contenido (`overflow: hidden`) el texto del Chip se cortaba a la mitad en vez de ajustarse — de ahí el aspecto de "doble botón" (la tarjeta con borde nuevo del hover, más el Chip truncado y desbordado encima). Se corrigió permitiendo que la etiqueta del Chip haga salto de línea (`whiteSpace: 'normal'`) en vez de desbordarse, y de paso se revirtió que esta tarjeta específica fuera clicable — el Chip ya comunica el estado con claridad y no aporta una acción nueva distinta a las otras tres, así que agregarle también el borde/elevación de hover solo sumaba ruido visual sin necesidad. Verificado visualmente con una captura real del navegador: el Chip ahora se ve completo, en dos líneas, dentro de los límites de la tarjeta.
- El usuario aclaró que el "doble botón" en realidad está en las tarjetas "Ingresos/Gastos/Ganancia neta" (no en "Estado del periodo"), y que el mismo problema ya existía en el Dashboard, no solo en Finanzas. Revisando el estado normal (sin cursor encima) con una captura real, esas tarjetas se ven limpias — ícono + texto, sin nada raro — así que el problema parece ser específico del estado `:hover`, que no se puede capturar de forma confiable con las herramientas automatizadas de este entorno (limitación ya documentada antes en esta sesión). La hipótesis más probable: el borde grueso (3px) de color primario sólido que aparecía al pasar el cursor, combinado con el ícono ya redondeado y de color (el avatar), hacía que se vieran DOS elementos con apariencia de botón a la vez (el avatar chiquito + el marco de toda la tarjeta). Se suavizó el efecto: borde de 1px con el color neutro `divider` (no un azul saturado) en vez de 3px en `primary.main`, más una sombra de elevación (`z16`) para transmitir "flotando" sin depender de un marco de color tan llamativo, y una elevación algo menor (-4px en vez de -6px). Se le pidió al usuario una captura de pantalla real para confirmar si esto lo resuelve, ya que el asistente no puede verificar visualmente el estado `:hover` de forma confiable en este entorno.
- El usuario reportó que con el borde de 1px "lo quitaste" — es decir, se pasó de "se ve como doble botón" a "ya no se nota nada", señal de que el ajuste anterior quedó demasiado sutil. Se eliminó por completo el cambio de color de borde (ya no compite visualmente con el ícono de la tarjeta) y se dejó únicamente una sombra fuerte y bien visible (`0 16px 32px -4px rgba(0,0,0,0.45)`, sin depender de ningún token de color del tema) más la elevación (`translateY(-6px)`) — el patrón más estándar y reconocible de "tarjeta que se levanta con una sombra debajo", sin ningún marco de color que pueda leerse como un segundo botón. Pendiente de confirmación visual del usuario.
- El usuario aclaró que en realidad quería que estas tarjetas se vieran como los botones de "Acciones rápidas" (con borde visible todo el tiempo, no solo al pasar el cursor), no solo que "flotaran" al hacer hover. Se le agregó un borde de 2px permanente. Al revisar el resultado con una captura real se encontró el bug exacto: el color usado (`divider`) resulta ser, en modo oscuro, exactamente el mismo color que el fondo de la propia tarjeta (`background.paper`) — ambos mapeados a `darkLevel1` desde el trabajo de modo oscuro de esta misma sesión — así que el borde estaba ahí, pero era literalmente invisible por diseño (mismo color que lo que hay detrás). Se cambió a `grey[500]` (un tono medio, pensado para contrastar contra el fondo en ambos modos), que al pasar el cursor se intensifica a `primary.main`, replicando el mismo lenguaje visual de los botones outlined. Al revisar con una captura de pantalla ya se ve un borde claro en las esquinas de la tarjeta, aunque no se pudo confirmar al 100% que se vea continuo en los cuatro lados por las mismas limitaciones de captura ya documentadas en esta sesión — se le pidió al usuario que confirme en su navegador real.
- El usuario mandó una captura real de su propio navegador mostrando el problema: en vez de un borde alrededor de la tarjeta, se veían solo un par de rayitas verticales sueltas, una arriba y otra abajo del contenido, sin nada en el medio — ni rastro de borde alrededor del ícono o el texto. Reproducido exactamente igual en un navegador headless completamente limpio (sin ningún estado previo), lo que confirmó que no era un problema de caché ni de sesión vieja del usuario, sino un bug real y reproducible. Se encontró la causa raíz con `getComputedStyle`: la tarjeta, al convertirse en enlace (`component={Link}`, que renderiza una etiqueta `<a>`), tenía `display: inline` — el valor por defecto de cualquier `<a>` en HTML si nada lo cambia explícitamente. Como adentro de ese `<a>` "en línea" hay contenido de bloque (el `Stack` con el ícono y el texto), el navegador parte la caja en línea en fragmentos alrededor del contenido de bloque — de ahí las dos rayitas sueltas arriba y abajo, y el borde nunca rodeaba visualmente el contenido real. Se corrigió forzando `display: 'flex'` en la regla `[href]` del override de `MuiCard`, lo mismo que ya tiene el `Card` por defecto cuando NO se usa como enlace (por eso este bug nunca se notó en tarjetas normales, solo en las que se volvieron clicables en esta sesión). Verificado con una captura de pantalla real: ahora se ve un borde completo, continuo, con las cuatro esquinas redondeadas, rodeando cada tarjeta — igual que un botón outlined.
- Se añadió el catálogo de "Servicios y mano de obra" (modelo `ServiceCatalogItem`, migración `add_service_catalog`): nombre, descripción opcional, costo interno opcional (para poder calcular margen igual que con las piezas del inventario) y precio al cliente. Nueva sección `/service-catalog` (dentro del menú "Inventario") con listado, alta/edición, margen calculado en vivo, y activar/desactivar sin borrar el historial de cotizaciones que ya lo usaron. Endpoints `GET/POST/PATCH /api/service-catalog` (Admin/Recepción para escritura, cualquier rol autenticado para lectura, igual que Inventario).

  El objetivo real era que, al armar una cotización, los precios salieran solos en vez de capturarlos a mano cada vez. El formulario de "Nueva cotización" ahora tiene un buscador (`Autocomplete` con `freeSolo`) que combina en un solo cuadro las piezas del Inventario y los servicios de este nuevo catálogo, agrupados por tipo y mostrando el precio de cada uno en la lista; al elegir uno, la descripción y el precio unitario se llenan solos (se pueden ajustar después si hace falta), y si no existe en ningún catálogo se puede seguir escribiendo un concepto libre con precio manual, sin perder esa flexibilidad. Verificado de punta a punta con un navegador real: se creó un servicio de prueba, se confirmó que aparece en el buscador de la cotización junto con una pieza real del inventario (agrupados correctamente, "Piezas" vs "Servicios y mano de obra"), y que al elegir el servicio el campo de precio unitario se llena automáticamente con el valor correcto. Se añadió cobertura e2e para el catálogo (alta, edición, activar/desactivar, permisos por rol).

### 2026-09-14

- A pedido del usuario ("continúa con las interfaces, según el flujo, que sean amigables al usuario, mientras menos teclar, mejor"), se redujo la captura manual en los dos formularios de más uso diario: "Nueva orden de servicio" y "Nueva cotización". Los campos "Marca" y "Modelo" pasaron de texto libre a `Autocomplete` con `freeSolo`, con sugerencias construidas a partir de los equipos que ya existen en la base de clientes (sin llamada nueva al backend, reutilizando los datos que `GET /api/customers` ya trae anidados); al escribir una marca conocida aparece como sugerencia con un clic, y al elegir un modelo que ya existe se autocompleta también la categoría del equipo (celular/tablet/laptop/consola) y, si hacía falta, la marca — sin dejar de poder escribir un equipo nuevo que no esté en el historial. Debajo de "Falla reportada" se agregó una fila de chips con las fallas más comunes del taller (pantalla rota, no enciende, no carga/batería, se mojó, botones no responden, cámara no funciona, audio falla, lento/se congela); un clic agrega la frase al texto (separada con "; " si ya había algo escrito), sin bloquear seguir escribiendo el resto a mano. Verificado con un navegador real con sesión iniciada: al escribir "Sam" en Marca aparece "Samsung" (de un cliente real ya registrado), y dos clics consecutivos en chips de falla arman correctamente "Pantalla rota; No enciende" en el textarea. `npm run build` y `eslint` limpios en ambos archivos.
- Se continuó la misma pasada de "menos teclar" en cuatro pantallas más. (1) La ficha de cliente ("Registrar equipo") recibió el mismo tratamiento de Marca/Modelo con `Autocomplete` y autocompletado de categoría que ya tenían Nueva orden y Nueva cotización, esta vez usando `api.listCustomers()` (todos los clientes, no solo el actual) para construir las sugerencias, ya que es aquí donde de hecho se da de alta cada equipo nuevo por primera vez. (2) En "Nueva pieza" y "Editar pieza" del Inventario, el campo "Categoría" (antes texto libre, con riesgo de crear categorías duplicadas por una letra distinta o un typo) pasó a `Autocomplete freeSolo` sugiriendo las categorías que ya existen en el catálogo, reutilizando la misma lista que ya alimenta los chips de filtro de esa pantalla. (3) En "Nueva orden de compra", al elegir una pieza del menú "Pieza" ahora se autocompleta el campo "Costo unitario" con el último costo registrado de esa pieza (se puede ajustar a mano si el proveedor cambió el precio), en vez de dejarlo en blanco esperando que alguien recuerde o busque el costo. (4) En "Agenda", el campo "Título" del formulario de nuevo evento ahora se sugiere solo a partir del tipo de evento y el cliente elegido (p. ej. "Entrega · Juan Pérez"), reordenando el formulario para que Tipo y Cliente vayan primero; la sugerencia solo se aplica mientras el usuario no haya escrito su propio título distinto, para no pisar una edición manual. Las cuatro se verificaron con un navegador real con sesión iniciada: sugerencia de categoría "Pantallas" al escribir "Pan"; autocompletado del costo unitario a "65" al elegir una pieza real de la orden de compra; sugerencia de marca "Samsung" al escribir "Sam" en la ficha de un cliente real; y título armado correctamente como "Entrega · Javier Mendoza" tras elegir tipo y cliente en la agenda. `npm run build` y `eslint` limpios en los cuatro archivos.
- Se llevó la misma idea al detalle de la orden de servicio, la pantalla que más usan los técnicos día a día. En "Falla y prioridad" se agregó la misma fila de chips de fallas comunes que ya tenían los formularios de alta, para poder ampliar o corregir la falla reportada sin escribirla completa de nuevo. En "Presupuesto", junto al campo "Mano de obra" se agregó un buscador contra el catálogo de "Servicios y mano de obra" (el mismo que ya alimenta las cotizaciones): al elegir un servicio, su precio se suma automáticamente al campo (en vez de reemplazarlo, por si se combinan varios servicios), dejando el monto final siempre editable a mano si hace falta un ajuste. Esto cierra el ciclo que pidió el usuario desde el inicio de esta serie de mejoras ("que al momento de hacer una cotización se muestren los precios en automático") para que también aplique al presupuesto real de la orden, no solo a la cotización previa. De paso se encontró y resolvió una fuente real de confusión para verificar cambios en este entorno: el `.env` del proyecto declara `VITE_API_URL=http://localhost:3001/api`, pero el servidor de desarrollo que ya estaba corriendo tenía en realidad `http://localhost:3002/api` (una variable de entorno del sistema con ese valor, de una sesión anterior, que Vite respeta por encima del archivo `.env`), y el puerto 3001 en este entorno está ocupado por el servidor de otro proyecto ajeno (`Nutri`) que también responde con JSON de error con forma similar a la de NestJS — cualquier verificación por `curl` directo a 3001 parecía "fallar" con 404 aunque la app funcionara perfectamente bien para el usuario real en el navegador (que sí usa la variable de entorno correcta a través de Vite). Verificado con un navegador real: clic en un chip de falla amplía correctamente el texto existente; al crear un servicio de prueba en el catálogo y elegirlo desde el buscador de la orden, "Mano de obra" pasó de "0" a "150.00" automáticamente; se confirmó que la orden real usada para la prueba no quedó modificada en la base de datos (la prueba solo cambia estado local del formulario, nunca se guardó) y se borró el servicio de prueba al terminar.
- Se cerró la pasada de "menos teclar" con dos pantallas más. (1) En "Usuarios y roles", el campo de contraseña al dar de alta un usuario nuevo ahora tiene un botón de dado que genera una contraseña segura de 12 caracteres con un clic (en vez de tener que inventar y teclear una), más botones para mostrarla en claro y copiarla al portapapeles, ya que quien la genera normalmente necesita leerla para compartirla con la persona nueva. (2) En "Base técnica", los campos "Marca"/"Modelo" al documentar una solución pasaron al mismo `Autocomplete` que ya tienen Nueva orden, Nueva cotización y la ficha de cliente, sugeridos a partir de los equipos ya registrados. Ambas verificadas con un navegador real: el dado generó una contraseña de 12 caracteres y el campo cambió a texto visible; "Marca" sugirió "Samsung" al escribir "Sam". También se investigó a fondo la causa raíz de la confusión de puertos documentada en la entrada anterior: el backend de NestJS no carga archivos `.env` por sí mismo (no usa `dotenv` ni `ConfigModule`), así que un `backend/.env` no tiene ningún efecto — las variables deben exportarse en el entorno antes de arrancar. El proyecto ya tenía la solución correcta desde antes en `dev.sh` (`npm run dev:all`): exporta todas las variables necesarias, detecta si el puerto 3001 está ocupado y usa el siguiente libre automáticamente (por eso terminó en 3002 en este entorno), aplica migraciones/seed, y levanta backend y frontend ya apuntando al puerto correcto. Se dejó un `.env.local` (ignorado por git, ya que `VITE_API_URL=http://localhost:3002/api`) para que `npm start` a secas también funcione en este entorno sin tener que usar `dev.sh` cada vez; quien lo necesite en otra máquina donde el puerto 3001 sí esté libre no necesita tocar nada. `npm run build` y `eslint` limpios en los dos archivos modificados.
- Una revisión automática de seguridad marcó el generador de contraseñas recién agregado: usaba `Math.random()`, que no es criptográficamente seguro (es predecible), un problema real para algo que genera credenciales. Se reemplazó por `crypto.getRandomValues` con muestreo por rechazo (para no introducir sesgo por el operador módulo). Verificado con un navegador real generando varias contraseñas seguidas: 12 caracteres, todas distintas, sin errores en consola.
- El usuario reportó "hay errores en el backend". Investigando, se encontró la causa raíz real (más allá del rodeo de puertos ya documentado): `backend/src/main.ts` nunca cargó archivos `.env` — ni `dotenv` ni `ConfigModule` de Nest estaban presentes —, así que el flujo de arranque que el propio `README.md` documenta desde el principio del proyecto (`cp .env.example .env` seguido de `npm run start:dev`) en realidad **nunca funcionó**: sin exportar las variables a mano en la terminal antes de correr el comando, Nest fallaba con `PrismaClientInitializationError: Environment variable not found: DATABASE_URL` sin más contexto. Esto coincide exactamente con lo que reportó el usuario: se encontró un segundo proceso `npm run start:dev` corriendo en una terminal interactiva real (no del asistente) sin ningún puerto abierto, señal de que había fallado en silencio por esta causa. Se agregó `dotenv` como dependencia declarada (ya estaba disponible de forma transitiva, pero no declarada) y un `import 'dotenv/config'` como primera línea de `main.ts`. Verificado de punta a punta: se creó un `.env` de prueba con un puerto distinto y se lanzó `npm run start:dev` en un entorno de shell completamente limpio (`env -i`, sin ninguna variable heredada) — el servidor arrancó y respondió correctamente solo con lo que traía el `.env`, algo que antes era imposible. Se restauró `backend/.env` (ignorado por git) con el puerto correcto para este entorno (3002). `npm run build`, `npm test`, `npx tsc --noEmit` (incluyendo las pruebas e2e) y `npm audit` quedaron limpios tras el cambio; la instancia ya corriendo vía `dev.sh` no se vio afectada (sus variables de entorno explícitas siguen teniendo prioridad sobre `.env`, que es el comportamiento estándar de `dotenv`).
- Tras ese arreglo, el usuario volvió a chocar con un `EADDRINUSE` al levantar su propio `dev.sh`: la causa esta vez fue que el asistente había dejado corriendo, desde una verificación anterior en esta misma sesión, su propia instancia completa de `dev.sh` (backend en 3002, frontend en 3000) sin apagarla al terminar. Cuando el usuario levantó la suya, `dev.sh` detectó 3001 y 3002 ocupados y pasó a 3003 para el backend, pero el frontend (Vite, con puerto fijo 3000 en `vite.config.mjs`) también chocó con el 3000 ya ocupado y se corrió a 3003 por su cuenta, provocando una carrera por el mismo puerto entre ambos procesos. Se mató la instancia sobrante del asistente y se le pidió al usuario reiniciar `dev.sh`, quedando resuelto. Lección operativa para el resto de la sesión: cualquier servidor de desarrollo que el asistente levante para verificar un cambio debe apagarse explícitamente al terminar esa verificación, sin excepciones, precisamente para evitar chocar con el entorno real del usuario.
- A pedido del usuario ("comencemos a mejorar el código usando buenas prácticas y optimizando"), se inició una limpieza de legibilidad del frontend: muchas vistas se habían ido escribiendo, sesión tras sesión, como una sola línea de JSX gigante (el peor caso, `customers/detail.jsx`, tenía una sola línea de código de casi 6000 caracteres), lo que las hacía casi imposibles de leer, revisar en diffs o editar con precisión quirúrgica. Se corrió el formateador `prettier` (ya configurado en el proyecto con `printWidth: 140`, nunca antes aplicado de forma consistente) sobre todo `src/`, seguido de `eslint --fix` para limpiar los últimos detalles de formato — un cambio puramente de forma, sin tocar ninguna lógica. Se verificó exhaustivamente que el comportamiento no cambió: `eslint` quedó en 0 errores (solo 2 advertencias preexistentes de dependencias de `useEffect`, ya presentes antes del formateo), `npm run build` compiló con los mismos tamaños de bundle, y se recorrieron con un navegador real las 11 pantallas principales más las vistas de detalle de cliente y de orden (las más afectadas por el cambio) sin un solo error de consola.
- Se atacó el tamaño del bundle, la segunda prioridad que eligió el usuario. El build ya avisaba de varios chunks pesados que se cargaban de más: `jspdf`/`jspdf-autotable` (417KB) y su dependencia transitiva `html2canvas` (201KB) se descargaban con solo **visitar** el detalle de una orden o cotización, aunque el usuario nunca tocara el botón "Descargar PDF" — porque `generateOrderPdf`/`generateQuotationPdf` se importaban de forma estática al inicio de esos archivos. Se cambió a `import()` dinámico dentro del propio manejador del botón, así esas ~600KB solo se descargan cuando de verdad se genera un PDF. De forma similar, `react-apexcharts` (583KB) se creó un envoltorio reutilizable (`ui-component/charts/LazyApexChart.jsx`, con `React.lazy` + `Suspense` y un `Skeleton` como marcador de posición) para que el Dashboard, Finanzas y Productividad por técnico pinten el resto de la pantalla (KPIs, tablas) de inmediato sin esperar a que la librería de gráficas termine de descargar y parsear — antes, al ser un `import` estático dentro de un componente ya lazy-cargado por ruta, el navegador tenía que esperar toda la gráfica antes de mostrar nada. Verificado con un navegador real registrando cada petición de red: visitar el detalle de una orden ya NO dispara ninguna petición de `jspdf`/`html2canvas`; hacer clic en "Descargar PDF" sí las dispara al momento, en ambas pantallas (orden y cotización), y el botón vuelve a su estado normal sin errores de consola en ningún caso; el Dashboard sigue cargando la gráfica (es la pantalla de entrada, así que no hay forma de evitarlo del todo) pero el contenido de las tarjetas ya aparece en pantalla de inmediato.
- Se revisó el backend en busca de estructura y duplicación (tercera prioridad del usuario) y se encontraron dos casos reales, no solo cosméticos. (1) La generación de folios (`OS-`, `COT-`, `OC-`, `GA-` para reclamos de garantía) estaba copiada y pegada de forma idéntica en cinco lugares (`service-orders`, `quotations` x2, `purchase-orders`) como `` `PREFIJO-${Date.now().toString().slice(-6)}` ``, y ese patrón tiene un bug real de robustez: al quedarse solo con los últimos 6 dígitos del timestamp en milisegundos, el valor se repite cíclicamente cada ~16.7 minutos, así que dos órdenes creadas con esa diferencia exacta de tiempo (algo perfectamente plausible en un taller con varias personas dando de alta órdenes durante el día) generarían el mismo folio y la segunda fallaría con un error de restricción única sin manejar (`folio` es `@unique` en los tres modelos). Se extrajo a `backend/src/common/folio.ts`, una sola función `generateFolio(prefijo)` usada en los cinco lugares, que agrega dos dígitos aleatorios al mismo formato numérico corto (pensado para mostrarse a clientes en recibos y tickets) — no elimina la colisión en el sentido matemático estricto, pero la vuelve realistamente segura para el volumen de este taller sin cambiar el aspecto ni el largo del folio de forma notoria. (2) La validación de tipo de archivo (imágenes JPG/PNG/WEBP, más PDF para adjuntos de orden) estaba repetida con el mismo arreglo de MIME types y el mismo `if (file && !tipos.includes(...)) throw new BadRequestException(...)` en tres controladores (`inventory`, `auth` para avatar, `service-orders` para adjuntos); se extrajo a `backend/src/common/file-validation.ts` (`IMAGE_MIME_TYPES`, `ATTACHMENT_MIME_TYPES`, `assertAllowedMimeType`), conservando exactamente los mismos mensajes de error. Verificado: `npm run build`, `npm test` y `npx tsc --noEmit` (incluyendo e2e) limpios; en vivo contra el backend real se creó una orden de compra real y se confirmó el nuevo formato de folio (`OC-57545585`), y se probó la subida de avatar tanto con un archivo de texto (rechazado con el mismo mensaje de siempre) como con un PNG real (aceptado) — todos los datos de prueba se limpiaron al terminar.
- Se cerraron las cuatro prioridades que eligió el usuario con la última: cobertura de pruebas unitarias del backend, que hasta ahora solo existían para `auth` (login). Se agregaron pruebas para los tres módulos que el propio usuario señaló como sin cubrir: `finance.service.spec.ts` (el más valioso de los tres, porque tiene lógica de cálculo real: ingresos, gastos, ganancia neta, el booleano `isProfit`, el margen — incluyendo el caso borde de margen 0% cuando no hay ingresos en vez de `NaN`/`Infinity`, la construcción de la lista de movimientos combinando pagos y compras ordenados por fecha, y que el rango de fechas solicitado sí se pase a ambas consultas), `service-catalog.service.spec.ts` (que el filtro por defecto excluya inactivos, que `includeInactive` sí los incluya, que la búsqueda por nombre sea insensible a mayúsculas, y que el costo interno se calcule en 0 si no se especifica) y `appointments.service.spec.ts` (que las citas nuevas siempre nazcan en estado `SCHEDULED` y que las fechas de texto se conviertan a objetos `Date` reales). Se siguió el mismo patrón ya usado en `auth.service.spec.ts` (Prisma simulado con `jest.fn()`, sin base de datos real, instanciando el servicio directamente) para que las pruebas corran en segundos sin depender de Postgres. Las 13 pruebas (4 archivos) pasan, y `npx tsc --noEmit` sigue limpio.

### 2026-09-15 (continuación)

- A pedido del usuario ("continúa puliendo la experiencia"), se buscaron acciones destructivas sin confirmación en toda la app. Se encontraron tres reales: archivar un cliente (usaba `window.confirm`, el diálogo nativo feo del navegador, inconsistente con el resto de la interfaz), eliminar una foto o documento de una orden de servicio (sin ninguna confirmación — un clic accidental borra evidencia real del equipo sin forma de deshacerlo) y cancelar una orden de compra (tampoco tenía confirmación, y es una decisión de negocio real). Se creó un componente reutilizable `ui-component/ConfirmDialog.jsx` (un `Dialog` de MUI simple con título, descripción y botón de confirmar en rojo) y se usó en las tres pantallas, reemplazando el `window.confirm` y agregando las dos confirmaciones que faltaban. Verificado de punta a punta con un navegador real: se confirmó que el diálogo nativo del navegador ya no aparece en ningún caso (se escuchó el evento `dialog` de Puppeteer, que solo dispara para diálogos nativos, y nunca sonó); se canceló el archivado de un cliente real y se comprobó que siguió activo; se subió una foto de prueba a una orden real, se confirmó su eliminación con el nuevo diálogo y desapareció correctamente; se creó una orden de compra de prueba, se canceló con el nuevo diálogo y se confirmó por API que quedó en estado `CANCELLED`. Todos los datos de prueba se limpiaron al terminar. `npm run build` y `eslint` limpios.
